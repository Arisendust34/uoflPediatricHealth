import { Message, SendSMSResponse } from '../../types';
export declare class MessageSendPartialFailure extends Error {
    protected response: SendSMSResponse;
    constructor(message: string, response: SendSMSResponse);
    getFailedMessages(): Message[];
    getResponse(): SendSMSResponse;
    getSuccessfulMessages(): Message[];
}
