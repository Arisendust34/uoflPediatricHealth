import { Client } from '@vonage/server-client';
import { SMSParams, SendSMSResponse } from './types';
export declare class SMS extends Client {
    send(params?: SMSParams): Promise<SendSMSResponse>;
}
