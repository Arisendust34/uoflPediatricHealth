import { TalkAction } from '../../interfaces/NCCO/TalkAction';
import { Serializable } from '../../ncco';
export declare class Talk implements TalkAction, Serializable {
    action: string;
    text: string;
    bargeIn?: boolean;
    loop?: number;
    level?: number;
    language?: string;
    style?: string;
    premium?: boolean;
    constructor(text: string, bargeIn?: boolean, loop?: number, level?: number, language?: string, style?: string, premium?: boolean);
    serializeToNCCO(): TalkAction;
}
