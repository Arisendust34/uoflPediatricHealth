import { PhoneEndpointObject } from '../../interfaces/Endpoint/PhoneEndpointObject';
export declare class PhoneEndpoint implements PhoneEndpointObject {
    type: string;
    number: string;
    dtmfAnswer?: string;
    constructor(phoneNumber: string, dtmfAnswer?: string);
}
