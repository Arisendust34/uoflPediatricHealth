import { MessageType } from '../../interfaces/Messenger/MessageType';
import { VideoObject } from '../../interfaces/VideoObject';
import { AbstractVideoMessage } from '../AbstractVideoMessage';
export declare class Video extends AbstractVideoMessage {
    channel: string;
    messenger?: MessageType;
    constructor(video: VideoObject, to: string, from: string, messenger?: MessageType, clientRef?: string);
}
