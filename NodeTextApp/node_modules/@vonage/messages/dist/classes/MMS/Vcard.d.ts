import { MessageObject } from '../../interfaces/MessageObject';
export declare class Vcard implements MessageObject {
    message_type: string;
    channel: string;
    vcard: {
        url: string;
    };
    to: string;
    from: string;
    client_ref?: string;
    constructor(vcardUrl: string, to: string, from: string, clientRef?: string);
}
