import { AudioMessage } from '../interfaces/AudioMessage';
import { AudioObject } from '../interfaces/AudioObject';
export declare abstract class AbstractAudioMessageObject implements AudioMessage {
    message_type: string;
    channel: string;
    audio: AudioObject;
    to: string;
    from: string;
    client_ref?: string;
    constructor(audio: AudioObject, to: string, from: string, clientRef?: string);
}
