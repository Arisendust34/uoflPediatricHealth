import { VideoMessage } from '../interfaces/VideoMessage';
import { VideoObject } from '../interfaces/VideoObject';
export declare abstract class AbstractVideoMessage implements VideoMessage {
    message_type: string;
    channel: string;
    video: VideoObject;
    to: string;
    from: string;
    client_ref?: string;
    constructor(video: VideoObject, to: string, from: string, clientRef?: string);
}
