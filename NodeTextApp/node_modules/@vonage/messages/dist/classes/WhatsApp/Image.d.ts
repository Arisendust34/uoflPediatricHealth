import { ImageObject } from '../../interfaces/ImageObject';
import { AbstractImageMessage } from '../AbstractImageMessage';
export declare class Image extends AbstractImageMessage {
    channel: string;
    constructor(image: ImageObject, to: string, from: string, clientRef?: string);
}
