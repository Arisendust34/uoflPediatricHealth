import { AudioObject } from '../../interfaces/AudioObject';
import { AbstractAudioMessageObject } from '../AbstractAudioMessage';
export declare class Audio extends AbstractAudioMessageObject {
    channel: string;
    constructor(audio: AudioObject, to: string, from: string, clientRef?: string);
}
