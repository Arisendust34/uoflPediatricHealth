import { MessageObject } from '../../interfaces/MessageObject';
import { MessageTemplate } from '../../interfaces/WhatsApp/MessageTemplate';
export declare class TemplateMessage implements MessageObject {
    message_type: string;
    channel: string;
    template: MessageTemplate;
    to: string;
    from: string;
    client_ref?: string;
    whatsapp: {
        policy: string;
        locale: string;
    };
    constructor(template: MessageTemplate, to: string, from: string, locale: string, clientRef?: string);
}
