import { FileObject } from '../../interfaces/FileObject';
import { AbstractFileMessage } from '../AbstractFileMessage';
export declare class File extends AbstractFileMessage {
    channel: string;
    constructor(file: FileObject, to: string, from: string, clientRef?: string);
}
