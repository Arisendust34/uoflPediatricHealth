import { MessageObject } from '../../interfaces/MessageObject';
import { CustomPayload } from '../../interfaces/WhatsApp/CustomPayload';
export declare class CustomMessage implements MessageObject {
    message_type: string;
    channel: string;
    custom: CustomPayload;
    to: string;
    from: string;
    client_ref?: string;
    constructor(custom: CustomPayload, to: string, from: string, clientRef?: string);
}
