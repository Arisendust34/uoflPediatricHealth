import { FileMessage } from '../interfaces/FileMessage';
import { FileObject } from '../interfaces/FileObject';
export declare abstract class AbstractFileMessage implements FileMessage {
    message_type: string;
    channel: string;
    file: FileObject;
    to: string;
    from: string;
    client_ref?: string;
    constructor(file: FileObject, to: string, from: string, clientRef?: string);
}
