import { TextMessage } from '../interfaces/TextMessage';
export declare abstract class AbstractTextMessageObject implements TextMessage {
    message_type: string;
    channel: string;
    text: string;
    to: string;
    from: string;
    client_ref?: string;
    constructor(text: string, to: string, from: string, clientRef?: string);
}
