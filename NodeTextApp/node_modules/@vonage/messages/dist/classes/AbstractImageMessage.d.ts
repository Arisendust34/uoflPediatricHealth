import { ImageMessage } from '../interfaces/ImageMessage';
import { ImageObject } from '../interfaces/ImageObject';
export declare abstract class AbstractImageMessage implements ImageMessage {
    message_type: string;
    channel: string;
    image: ImageObject;
    to: string;
    from: string;
    client_ref?: string;
    constructor(image: ImageObject, to: string, from: string, clientRef?: string);
}
