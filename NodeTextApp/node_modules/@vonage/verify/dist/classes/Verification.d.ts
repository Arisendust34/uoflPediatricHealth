import { VerificationRequest } from '../interfaces/VerificationRequest';
export declare class Verification implements VerificationRequest {
    number: string;
    country?: string;
    senderId?: string;
    codeLength?: number;
    language?: string;
    pinExpiry?: number;
    nextEventWait?: number;
    workflowId?: number;
    constructor(phoneNumber: string, country?: string, senderId?: string, codeLength?: number, language?: string, pinExpiry?: number, nextEventWait?: number, workflowId?: number);
}
